Load the ux-session-planner skill from `.claude/skills/ux-session-planner.md` and execute it.

Argument: the scenario ID (e.g., `ux-session-001`). Read the scenario from `ux-sessions/scenarios/<scenario-id>.md`, read party profiles from `ux-sessions/party.md`, build a 7-slave plan for the UX exploration session, and launch.

If no argument provided, list available scenarios from `ux-sessions/scenarios/` and ask which to run.
